package main
import "fmt"

func main(){
	var x string
	x = "Hello, World"
	fmt.Println(x)
	var y string
	y = "let's go"
	fmt.Println(y)
	fmt.Println(x == y)
	z := "This is another way of variable declaration in go"
	fmt.Println(z)
	// scope of variables
}
